version: 2
jobs:
  build:
    docker:
      - image: circleci/openjdk:stretch

    steps:
      - checkout

      - run:
          name: Setup Environment
          command: ... 
        
      - run:
          name: Build Parser 
          command: echo Hello, world.

      - run:
          name: Test Spthy
          command: date